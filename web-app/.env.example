# En local (dev) : URL complète du backend
# En production (VM Azure, nginx sert web + API sous le même domaine) :
# utiliser un chemin relatif -> VITE_API_URL=/api
VITE_API_URL=http://localhost:8080/api
